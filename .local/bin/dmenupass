#!/bin/sh

# This script is the SUDO_ASKPASS variable, meaning that it will be used as a
# password prompt if needed.

dmenu -fn "JetbrainsMono Nerd Font:style=Medium:size=16" -sb "#005577" -sf "#b8bb26" -nf "#98971a" -nb "#98971a" -p "$1" <&- && echo
